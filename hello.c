#include <stdio.h> //This a library function (Standard input out)

int main()//This is a function, its the entry point of every program
{
    printf("Hello C Language");//This is used to print out data
    return 0;//The return 0 statement, returns execution status to the OS. The 0 value is used for successful execution and 1 for unsuccessful execution
}