#include <stdio.h>
/* This program prints the ASCII you want*/

int main()
{
    char ch; //varuable declearation
    printf("Enter a character");
    scanf("%c",&ch);  // user input
    printf("\n The ascii value of the ch variable is : %d", ch);
    return 0;
}
